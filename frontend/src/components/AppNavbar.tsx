import React from 'react';
import { Navbar } from 'react-bootstrap';

const AppNavbar = () => (
  <Navbar bg="dark" variant="dark">
    <Navbar.Brand href="#home">Chtracker</Navbar.Brand>
  </Navbar>
);

export default AppNavbar;
